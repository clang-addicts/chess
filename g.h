#ifndef __G_H__ // global variables
#define __G_H__

enum {
    MENU_EXIT = 0,
    MENU_INFO,
    MENU_SETTINGS,
    MENU_PLAY_SINGLE,
    MENU_PLAY_ONLINE,
    MENU_MAX
};

enum {
    NMODE_EXIT = 0,
    NMODE_SERVER,
    NMODE_CLIENT,
    NMODE_MAX
};

enum {
    GAMEMODE_CPU = 0,
    GAMEMODE_PLAYER,
    GAMEMODE_MAX
};

#endif // __G_H__